server:
  port: 8070
spring:
  application:
    name: server-api

eureka:
  instance:
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
feign:
  hystrix:
    enabled: true #打开hystrix在feign的开关