server:
  port: 8004
spring:
  application:
    name: consumer-u
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true   #注册服务的时候使用服务的IP地址